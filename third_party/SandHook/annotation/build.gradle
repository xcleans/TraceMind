plugins {
    id 'java-library'
}

java {
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11
}

// JitPack / Maven：与 atrace-core 链式发布到 ~/.m2（见根工程 publishToMavenLocal）
apply from: rootProject.file('gradle/publish-atrace-java.gradle.kts')
