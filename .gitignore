./local.properties
./build
.gradle
.kotlin
.idea
.cursor
.conversation
./atrace.workspace
.kotlin
.idea
.venv
./maven-repo
log/
__pycache__/
*.pyc

# atrace-tool.jar is deployed to atrace-provision/atrace_provision/bundled_bin/
# (./gradlew deployMcp). Track that JAR in git if you ship standalone wheels.
# atrace-mcp/bin/ may only contain a pointer README.